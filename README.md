# evidently_users

Usage

```
# Basic Use
git clone git@github.com:aetiologicCanada/evidently_users.git
cd evidently_users
cd demotestuser_01
tar -xvf evidently_credentials.tar

# Run Tests
git submodule init
git submodule update
chmod 700 *.sh
sudo ./test.sh demotestuser_01
```
